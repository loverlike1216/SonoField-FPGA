# Historical v1 evidence copied intact

These files record v1 runs before migration. Paths, version words, source hashes and statuses are historical.
They are not v2 validation and are not runtime inputs. Current v2 results live in adjacent validation,
model, reproducibility and bootstrap directories. The original v1 tree remains frozen and byte-identical.
